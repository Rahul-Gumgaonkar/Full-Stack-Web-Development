// Prompt is basicaly use for taking the input

const a = Number(prompt("enter the value of a : "));
const b = Number(prompt("enter the value of b : "));

// alert is basically use for output

alert("Sum of a and b is : " + (a + b));

console.log("Sum of a and b is : " + (a + b));

let age = 18;
let status = age >= 18 ? "I can vote" : "I can not vote";
console.log(status);
