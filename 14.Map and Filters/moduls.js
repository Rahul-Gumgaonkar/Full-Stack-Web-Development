export function addition(a, b) {
  return a + b;
}

export const subtraction = (a, b) => {
  return a - b;
};
// default ma hamesha function use karna hai
export default function multiplication(a, b) {
  return a * b;
}
